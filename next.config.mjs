/** @type {import('next').NextConfig} */
const nextConfig = {
  trailingSlash: false,
  // for statig
  // output: "export",
  // images: {
  //   unoptimized: true,
  // },
};

export default nextConfig;
